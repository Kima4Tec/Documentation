# .subscribe

**I Angular’s HttpClient er der forskellige callback:**

next = hvad der skal ske, når du får et rigtigt svar fra API’et

error = hvad der skal ske, hvis API’et giver en fejl

complete = kører når observable er færdig (sjældent brugt ved HTTP)
